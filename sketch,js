function setup() {
  createCanvas(1000, 800);
 background("rgb(54,250,54)");
}
function draw() {
  
   fill("rgb(255,0,0)"); 
    stroke("lightblue");
  if (mouseIsPressed)
     rect(mouseX,mouseY,30,50); 
} 
